name: Lorenz96 # needs to be spelled the exact same way like the class based on this
non_linearity: # mapped to external forcing parameter: lower values -> more stable
  # at F < 4 the system is stable
  low: 5
  med: 8
  hig: 10
chosen_non_linear_level: med # TODO add this to the code
initial_state: resources/initial_states/lorenz96_dim-36_delta-05_timesteps-14400.json
params: # parameters individual to the 
  delta_t: 0.05 # time step size
  system_dim: 36 # number of variables in system
  # TODO add list of params